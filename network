Analisando o arquivo de configuração Cisco, identifiquei várias senhas criptografadas com o algoritmo Type 7 (fracamente criptografado) que podem ser descriptografadas.

## 🔍 **Senhas encontradas no arquivo:**

### **1. Senhas Type 7 (descriptografáveis):**

```
username hub password 7 025017705B3907344E
username admin privilege 15 password 7 10181A325528130F010D24  
username guest password 7 124F163C42340B112F3830
line con 0 password 7 144101205C3B29242A3B3C3927
```

### **2. Senha Type 5 (mais forte):**
```
enable secret 5 $1$p8Y6$MCdRLBzuGlfOs9S.hXOp0.
```

## 🛠️ **Para descriptografar as senhas Type 7:**

### **Método 1 - Usando ferramentas online:**
- https://www.ifm.net.nz/cookbooks/passwordcracker.html
- https://tools.kali.org/password-attacks/cisco-passwords

### **Método 2 - Usando Python localmente:**

```python
def decrypt_type7(encrypted):
    # Chave de descriptografia Cisco Type 7
    key = [0x64, 0x73, 0x66, 0x64, 0x3b, 0x6b, 0x66, 0x6f, 
           0x41, 0x2c, 0x2e, 0x69, 0x79, 0x65, 0x77, 0x72, 
           0x6b, 0x6c, 0x64, 0x4a, 0x4b, 0x44, 0x48, 0x53, 
           0x55, 0x42, 0x73, 0x67, 0x76, 0x63, 0x61, 0x36, 
           0x39, 0x38, 0x33, 0x34, 0x6e, 0x63, 0x78, 0x76, 
           0x39, 0x38, 0x37, 0x33, 0x32, 0x35, 0x34, 0x6b, 
           0x3b, 0x66, 0x67, 0x38, 0x37]
    
    # Remove espaços e converte para bytes
    encrypted = encrypted.replace(" ", "")
    plaintext = ""
    
    # Pega o primeiro byte como seed
    seed = int(encrypted[0:2], 16)
    
    # Descriptografa o resto
    for i in range(2, len(encrypted), 2):
        hex_val = int(encrypted[i:i+2], 16)
        plaintext += chr(hex_val ^ key[seed])
        seed += 1
    
    return plaintext

# Teste as senhas
passwords = [
    "025017705B3907344E",
    "10181A325528130F010D24", 
    "124F163C42340B112F3830",
    "144101205C3B29242A3B3C3927"
]

for pwd in passwords:
    print(f"Encrypted: {pwd}")
    print(f"Decrypted: {decrypt_type7(pwd)}")
    print("---")
```

### **Método 3 - Usando john the ripper:**
```bash
# Criar arquivo com as senhas
echo "enable secret 5 \$1\$p8Y6\$MCdRLBzuGlfOs9S.hXOp0." > cisco.txt
echo "username hub password 7 025017705B3907344E" >> cisco.txt
echo "username admin privilege 15 password 7 10181A325528130F010D24" >> cisco.txt

# Quebrar com john
john --format=md5crypt-long cisco.txt
```

## 🎯 **Resultados esperados:**

Baseado no padrão Cisco Type 7, as senhas provavelmente serão:

- **hub**: Senha mais simples
- **admin**: Senha privilegiada (provavelmente a que você precisa)
- **guest**: Senha de convidado  
- **console**: Senha da porta console

## 💡 **Dica importante:**

A senha **enable secret** (Type 5) é mais difícil de quebrar, mas as senhas **Type 7** são facilmente reversíveis. **Tente primeiro descriptografar as senhas Type 7** usando as ferramentas mencionadas.

**Qual senha você conseguiu descriptografar primeiro?** Normalmente a do usuário `admin` é a que dá acesso privilegiado ao switch.
